dependencies {
    compile project(':core')

    compile 'com.googlecode:kryo:1.04'
    compile 'de.ruedigermoeller:fst:1.28'
}

task runO(dependsOn: classes, type: JavaExec) {
    main = 'com.mojang.blixtser.benchmark.SerializerPerformanceTest';
    classpath = sourceSets.main.runtimeClasspath;
}
